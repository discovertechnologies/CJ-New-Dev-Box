<?xml version="1.0" encoding="UTF-8"?><record_update table="sp_widget">
    <sp_widget action="INSERT_OR_UPDATE">
        <category>standard</category>
        <client_script><![CDATA[// Copyrights belong to Discover Technologies LLC. © 2022 Discover Technologies LLC
/* Special License Rights
*  The Government's rights to use, display, or disclose this software are restricted.
*  Any reproduction of computer software, computer software documentation,
*  or portions thereof marked with this legend must also reproduce the markings. */
// Script Continued Below
function ($scope, spUtil, $rootScope, $timeout, spAriaUtil, spGtd, $window) {		
	var c = this;

	$scope.collapse = function() {
		$rootScope.$emit('sp-navbar-collapse');
	}
	$scope.loadingIndicator = $rootScope.loadingIndicator;
	$scope.itemAddedTooltipOpen = false;
	$scope.accessibilityEnabled = spAriaUtil.g_accessibility === "true";

	var cancelTooltipPromise;
	$scope.$on('sp_loading_indicator', function(e, value) {
		$scope.loadingIndicator = value;
	});
	// PRB1108244: visibleItems array is used to improve keyboard nav
	// in menu, refresh it as needed
	$scope.$watch('data.menu.items', function() {
		$scope.visibleItems = [];
		if ($scope.data.menu.items) {
			for (var i in $scope.data.menu.items) {
				var item = $scope.data.menu.items[i];
				if (item.items || (item.scriptedItems && item.scriptedItems.count != 0)){
					$scope.visibleItems.push(item);
				}
					
			}
		}
	}, true);

	// set the user's default home page
	c.pageChanged = function(item){
		if(item.src == "Task24.svg" && 
			c.data.settings.userDefaultLandingPage != "my_task_home_page"){
			$scope.data.newPage = "my_task_home_page";
			$scope.data.pageChanged=true;
		} else if(item.src == "Office24.svg" && 
			c.data.settings.userDefaultLandingPage != "my_ous_tasks_home_page"){
			$scope.data.newPage = "my_ous_tasks_home_page";
			$scope.data.pageChanged=true;
		}
		if(	$scope.data.pageChanged==true) {
			spUtil.update($scope);
		}
	}
	
	// Get list of record watchers
	var record_watchers = [];
	if ($scope.data.menu.items) {
		for(var i in $scope.data.menu.items) {
			var item = $scope.data.menu.items[i];
			if (item.type == 'scripted') {
				if (item.scriptedItems.record_watchers)
					record_watchers = record_watchers.concat(item.scriptedItems.record_watchers);
			}
			if (item.type == 'filtered') {
				record_watchers.push({'table':item.table,'filter':item.filter});
			}
		}
	}
	// Init record watchers
	for (var y in record_watchers){
		var watcher = record_watchers[y];
		spUtil.recordWatch($scope, watcher.table, watcher.filter);
	}
  $rootScope.$broadcast('sp-header-loaded');
}]]></client_script>
        <controller_as>c</controller_as>
        <css>.dtech-header-menu-items{
  display: flex;
  flex-flow: row wrap;
  justify-content:center;
  align-items:start;
}
/* the divider does not apply the hover state or selected*/
.dtech-header-menu-divider-tab {/*Tab Styling*/
  display: inline-block;
  /* needed height to get border outside the svg*/
  height: 42px;
  margin: 0 0 -1px;
  cursor: none;
  padding: 0px 15px 0px 0px;
  font-weight: 400;
  text-align: center;
  color: rgba(0, 0, 0, 1);
}

.dtech-header-menu-divider-tab &gt; a {
  cursor:default;
}

.dtech-menu-label-selected &gt; a {
  border-bottom: 5px solid rgba(20, 146, 230, 0.5);
}

.dtech-menu-color {
  color:$tasker-color-blue-dark-35363a
}
.header-loader {
	float: left;
  	width: 24px;
  	position: relative;
  	top: 24px;
}</css>
        <data_table>sp_instance_menu</data_table>
        <demo_data/>
        <description/>
        <docs/>
        <field_list>title</field_list>
        <has_preview>false</has_preview>
        <id>tasker-header-menu</id>
        <internal>false</internal>
        <link><![CDATA[function(scope, elem) {
	$(elem).on('keydown','.header-menu-item',function(e){
		var target = e.target;
		if (target.localName == 'a') {
			var currentMenuItem = $(target).parents('li');
			var parentUL = currentMenuItem.parents('ul');
			var allMenuItems = parentUL.children('li:visible');
			var firstMenuItem = allMenuItems.first();
			var lastMenuItem = allMenuItems.last();
			var menuItemToFocus;
			if(e.keyCode == 37) {
				e.preventDefault();
				currentMenuItem.removeClass('open');
				var previousMenuItem = currentMenuItem.prevAll('li:visible').first();
				//enable circular navigation
				menuItemToFocus = previousMenuItem.length ? previousMenuItem : lastMenuItem;
				menuItemToFocus.find('a').focus();
			} else if(e.keyCode == 39) {
				e.preventDefault();
				currentMenuItem.removeClass('open');
				var nextMenuItem = currentMenuItem.nextAll('li:visible').first();
				//enable circular navigation
				menuItemToFocus = nextMenuItem.length ? nextMenuItem : firstMenuItem;
				menuItemToFocus.find('a').focus();
			}
			if(e.keyCode == 9) {
				$(target).parents('li').removeClass('open');
			}
		}
		});
	}]]></link>
        <name>Tasker - Header Menu</name>
        <option_schema/>
        <public>false</public>
        <roles>x_153465_discovert.tasker_user</roles>
        <script><![CDATA[// Copyrights belong to Discover Technologies LLC. © 2022 Discover Technologies LLC
/* Special License Rights
*  The Government's rights to use, display, or disclose this software are restricted.
*  Any reproduction of computer software, computer software documentation,
*  or portions thereof marked with this legend must also reproduce the markings. */
// Script Continued Below
//------------------------------------------------------------------------------------------------------------------------
// COMPANY: DISCOVER TECHNOLOGIES
// CUSTOMIZED BY: ROBERT JOY
// WIDGET BASE: HEADER MENU
//------------------------------------------------------------------------------------------------------------------------
//
// 2.7.0 | 2021-11-01 | raj | STRY0114307 - 1. Artifact Story: Tasker Portal Page- Navigation Bar- New icons update
//                                         Changes made in tasker-menuTemplate and Server Script
// 2.7.2 | 2021-01-27 | raj | DFCT0010774 - New Menu Items with Glyph do not align with drop down menu items with the Discover SVG
//                                         Changes made in tasker-menuTemplate
// 2.8   | 2021-04-14 | raj | STRY0117600 - Update Templates to match the UI of Org Hierarchy 
//                                         Changes made in Server Only removed the Org Hierarchy hard coded values as it is built in script now
// 2.8   | 2022-04-27 | raj | STRY0120910 - Layout Selector (Org Hierarchy)- Add Fullscreen Tree as an option
//                                         Changes made in tasker_menuTemplate - added a check for condition on each item
// 2.8   | 2022-05-15 | raj | STRY0118963 - Homepage to Org Hierarchy- System remembers user in Bifold/Fullscreen/Table/List
//                                         Changes made in Server Script
// 
//------------------------------------------------------------------------------------------------------------------------
(function($sp, input, data, options, gs) {
	// STRY0118963 - updated to follow our standard format
	main();
	
	function main(){
		// build menus
		var menu_id = $sp.getValue('sys_id'); // instance sys_id
		var taskWidgetIconsAndMessage = new TaskerWidgetIconsAndMessages('tasker-header-menu');
		if(input){
			// if the filter changed, save it back
			if(input.pageChanged){
				taskWidgetIconsAndMessage.savePreferenceUserDefaultLandingPage(input.newPage);
			}
		}
		data.settings = taskWidgetIconsAndMessage.loadDefaultsAndSettings($sp);
		data.messages = taskWidgetIconsAndMessage.loadMessages();
		data.icons = taskWidgetIconsAndMessage.loadIcons();
		
		
		// STRY0114307 - need sys_id for links
		data.sys_id = $sp.getParameter("sys_id");

		getMenuItems(menu_id);

		// STRY0114307
		setItemSrcOnAllDescendants(data.menu.items);

		data.isLoggedIn = GlideSession.get().isLoggedIn();		
	}

	function getMenuItems(menu_id){
		var gr = new GlideRecord('sp_instance_menu');
		gr.get(menu_id);
		data.menu = {};
		data.menu.items = $sp.getMenuItems(menu_id);		
	}
	
	function setItemSrcOnAllDescendants(items){
		// STRY0114307
		for (var i in items){
			var item = items[i];
			// STRY0118963 - change how this works to allow different URL to be loaded
			var itemLabel = item.label.toString();
			if(itemLabel.toLowerCase().substring(0,6) == 'dtech-'){
				// using the SVG name in the menu item allows us to remove hard coding
				// we can use the svg name to set the svg
				var svgName = itemLabel.substring(6);
				// we can also use svg name to get the title from a message
				var icon = data.icons[svgName];
				item.src = icon.src;
				item.label = icon.title;
				// STRY0118963 - get the updated URL for org
				switch (icon.src){
					case "OrgHierarchy16.svg":
						if(data.settings.orgUserDefaultPageId){
							item.href = '?id=' + 
								data.settings.orgUserDefaultPageId + 
								'&table=x_153465_discovert_discover_office_hierarchy&spa=1&view=sp';
						}
						break;
					case "Divider_Black.svg":
						item.isDivider = true;
						break;
					case "Task24.svg":
						if(data.settings.userDefaultLandingPage == "my_task_home_page"){
							item.isSelectedPage = true;
						}
						break;
					case "Office24.svg":
						if(data.settings.userDefaultLandingPage == "my_ous_tasks_home_page"){
							item.isSelectedPage = true;
						}
						break;
				}
			}
			// if item has href and doesn't have sys_id in href, add the id
			if(item.href && item.href.indexOf("sys_id") == -1){
				item.href = item.href + "&sys_id=" + data.sys_id;
			}
			
			// if item has items recurse
			if(item.items && item.items.length > 0){
				setItemSrcOnAllDescendants(item.items);
			}
			// process the scripted items also
			if(item.scriptedItems && item.scriptedItems.items && item.scriptedItems.items.length > 0){
				setItemSrcOnAllDescendants(item.scriptedItems.items);
			}
		}

	}
})($sp, input, data, options, gs);]]></script>
        <servicenow>false</servicenow>
        <sys_class_name>sp_widget</sys_class_name>
        <sys_created_by>rjoy</sys_created_by>
        <sys_created_on>2021-12-02 00:22:08</sys_created_on>
        <sys_id>8359a1efdbc845101e1792c5d496192f</sys_id>
        <sys_mod_count>203</sys_mod_count>
        <sys_name>Tasker - Header Menu</sys_name>
        <sys_package display_value="Tasker" source="x_153465_discovert">c6b1162d4fc10300303dc3818110c7b1</sys_package>
        <sys_policy/>
        <sys_scope display_value="Tasker">c6b1162d4fc10300303dc3818110c7b1</sys_scope>
        <sys_update_name>sp_widget_8359a1efdbc845101e1792c5d496192f</sys_update_name>
        <sys_updated_by>rjoy</sys_updated_by>
        <sys_updated_on>2022-06-02 02:34:52</sys_updated_on>
        <template><![CDATA[<ul  aria-label="${Header menu}" role="menubar">
  <div class="header-loader" ng-class="{'invisible': !loadingIndicator}">
    <div class="hidden-xs sp-loading-indicator la-sm">
      <div></div>
      <div></div>
      <div></div>
    </div>
  </div>
<!--  
  <li ng-repeat="item in visibleItems" 
      class="dtech-header-menu-tab"
      ng-class="{dropdown: item.items.length > 0, 'header-menu-item': true}"
      ng-include="'tasker_menuTemplate'" 
      role="presentation">
	</li>
-->
  <li ng-repeat="item in visibleItems" 
      ng-click="c.pageChanged(item);"
      ng-class="{dropdown: item.items.length > 0, 
                'dtech-header-menu-tab': item.isDivider!=true, 
                'dtech-header-menu-divider-tab': item.isDivider==true,
                'dtech-menu-label-selected': item.isSelectedPage==true}"
      ng-include="'tasker_menuTemplate'" 
      role="presentation"
      >
  
      
      
    </li>
</ul>]]></template>
    </sp_widget>
</record_update>
